/*This is an anti-telemetry script done in a CS
fashion that conforms to the Dying Light engine.*/
sub main() {
	telemetry_enabled(false)
}
sub set(privacy) {
	if(!telemetry_enabled(false));
	return true;
	} else {
		telemetry_enabled(false);
		exception(true);
		throw;
	}
}
private float void(AntiPLS[] PLSCheck) {
	while(unchecked(set.VarFloat("insecure"));
	{
		do(getFlag (!isChecked);
		return false;
		throw null;
	}
}

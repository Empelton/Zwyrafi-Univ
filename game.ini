sub main()
{
	GameClassName("GameDI");
	LevelClassName("LevelDI");
	GameDLL("gamedll"); //The gamedll name. Do not touch unless you have DL source access.
	Locale("En"); //Locale
    //FastInitializePDB(0); //It is recommended not to use this as it may cause stability problems.
	GameName("Zwyrafi-Univ-DVXK"); //The name of the game. We can use this to display version names on full-load clients.
	GameExeCoreName("DyingLight");
	TitleId("5752083F");
	AppId(239140);
	LowViolence(0); //This should be disabled.
	//CrashCanShowMessageBox(1);

	//TextureStreamMem(0); //Removed as it causes FPS drops.

	
    SkuOverride("DURANGO_JP")
    {
        LowViolence(0);
    }
    
    SkuOverride("ORBIS_JP")
    {
        LowViolence(0);
    }
}
